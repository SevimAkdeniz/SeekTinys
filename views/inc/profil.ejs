<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Profil Sayfası</title>
  <link rel="stylesheet" href="/css/profile.css">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f3f4f6;
      margin: 0;
      padding: 20px;
    }

    .profile-container {
      max-width: 800px;
      margin: auto;
      background: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .profile-header {
      text-align: center;
      margin-bottom: 30px;
    }

    .profile-header h1 {
      margin-bottom: 10px;
    }

    .info-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
      padding: 8px 0;
      border-bottom: 1px solid #e5e7eb;
    }

    .info-row label {
      font-weight: bold;
      color: #555;
    }

    .buttons {
      display: flex;
      gap: 15px;
      justify-content: center;
      margin-top: 30px;
    }

    .buttons a {
      background-color: #4f46e5;
      color: white;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      transition: background-color 0.3s;
    }

    .buttons a:hover {
      background-color: #4338ca;
    }
  </style>
</head>
<body>
  <div class="profile-container">
    <div class="profile-header">
      <h1>👤 <%= user.username %></h1>
      <p>Rol: <%= user.role %></p>
    </div>

    <div class="info-row">
      <label>Email:</label>
      <span><%= user.email %></span>
    </div>
    <div class="info-row">
      <label>Telefon:</label>
      <span><%= user.phone || 'Belirtilmemiş' %></span>
    </div>
    <div class="info-row">
      <label>Kayıt Tarihi:</label>
      <span><%= user.createdAt.toLocaleDateString('tr-TR') %></span>
    </div>

    <div class="buttons">
      <% if (user.role === 'owner') { %>
        <a href="/host/ilanlarim">📋 İlanlarım</a>
        <a href="/host/rezervasyonlar">📅 Rezervasyonlar</a>
      <% } else if (user.role === 'customer') { %>
        <a href="/rezervasyonlarim">📅 Rezervasyonlarım</a>
      <% } %>
    </div>
  </div>
</body>
</html>
